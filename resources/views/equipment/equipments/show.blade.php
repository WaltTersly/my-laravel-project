@extends('layouts.app')

@section('content')
    <div class="container">
        <div class="row">
            @include('admin.sidebar')

            <div class="col-md-9">
                <div class="card">
                    <div class="card-header">Equipment {{ $equipment->id }}</div>
                    <div class="card-body">

                        <a href="{{ url('/equipment/equipments') }}" title="Back"><button class="btn btn-warning btn-sm"><i class="fa fa-arrow-left" aria-hidden="true"></i> Back</button></a>
                        <a href="{{ url('/equipment/equipments/' . $equipment->id . '/edit') }}" title="Edit Equipment"><button class="btn btn-primary btn-sm"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit</button></a>

                        <form method="POST" action="{{ url('equipment/equipments' . '/' . $equipment->id) }}" accept-charset="UTF-8" style="display:inline">
                            {{ method_field('DELETE') }}
                            {{ csrf_field() }}
                            <button type="submit" class="btn btn-danger btn-sm" title="Delete Equipment" onclick="return confirm(&quot;Confirm delete?&quot;)"><i class="fa fa-trash-o" aria-hidden="true"></i> Delete</button>
                        </form>
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $equipment->id }}</td>
                                    </tr>
                                    <tr><th> Equipmentname </th><td> {{ $equipment->Equipmentname }} </td></tr><tr><th> State Of Equipment </th><td> {{ $equipment->State_of_equipment }} </td></tr><tr><th> Equipment Location </th><td> {{ $equipment->Equipment_location }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
